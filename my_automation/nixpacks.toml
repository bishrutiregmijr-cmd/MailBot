[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python3.12 -m pip install -r requirements.txt"]

[start]
cmd = "python3.12 manage.py migrate && gunicorn my_automation.wsgi:application --bind 0.0.0.0:$PORT"