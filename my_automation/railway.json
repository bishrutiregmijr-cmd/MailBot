{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd my_automation && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd my_automation && python manage.py migrate && gunicorn my_automation.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}